import { newStorage } from "./storage";

export async function uploadImageFromUrl(url: string, key: string, bucket?: string) {
  // try {
  //   const storage = newStorage();
    
  //   const result = await storage.downloadAndUpload({
  //     url,
  //     key,
  //     bucket,
  //     contentType: "image/jpeg", // Default to JPEG, can be overridden if needed
  //     disposition: "inline"
  //   });

  //   return result;
  // } catch (error) {
  //   console.error("Failed to upload image from URL:", error);
  //   throw error;
  // }
} 